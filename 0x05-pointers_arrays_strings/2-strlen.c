#include <stdio.h>

/**
 *  _strlen - Returns the length of a string.
 * @str: The string of the length of 
 * return: The length of @str.
 */

{
	size_t length = 0;

	while (*str++)
	length++;
	return (length);

}

